[package]
name = "muginn-verify"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
muginn-core = { path = "../core" }
muginn-crypto = { path = "../crypto" }
muginn-adapters = { path = "../adapters" }
